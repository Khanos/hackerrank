{
    "version": "0.2.0",
    "configurations": [
        {
            "type": "node",
            "name": "Run Current File",
            "request": "launch",
            "program": "${file}"
        }
    ]
}